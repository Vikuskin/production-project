import { counterReducer } from './model/slices/counterSlice';
import type { ICounter } from './model/types/counter';
import { Counter } from './ui/Counter';

export { ICounter, counterReducer, Counter };
